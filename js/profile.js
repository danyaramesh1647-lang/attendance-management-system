const client=window.supabaseClient;const esc=s=>String(s??"").replace(/[&<>"']/g,c=>({"&":"&amp;","<":"&lt;",">":"&gt;",'"':"&quot;","'":"&#039;"}[c]));const ini=s=>(s||"U").split(/\s+/).slice(0,2).map(x=>x[0]).join("").toUpperCase();async function init(){const u=await client.auth.getUser();if(!u.data.user){location.href="index.html";return}const p=await client.from("profiles").select("*").eq("id",u.data.user.id).single();if(!p.data){location.href="index.html";return}const admin=p.data.role==="admin";document.getElementById("app").innerHTML=`<aside class="sidebar"><div class="brand"><span class="logo">A</span><b>AttendCloud</b></div><nav><a class="nav" href="dashboard.html">⌂ Overview</a>${admin?'<a class="nav" href="students.html">♙ Students</a><a class="nav" href="attendance.html">✓ Mark Attendance</a>':''}<a class="nav" href="reports.html">▤ Reports</a><a class="nav active" href="profile.html">◎ Profile</a></nav></aside><main class="content"><header class="topbar"><div><p class="eyebrow">ACCOUNT</p><h1>Profile</h1></div></header><div class="card profile-box"><div class="avatar big">${ini(p.data.full_name)}</div><div><h2>${esc(p.data.full_name)}</h2><p class="muted">${esc(p.data.email)}</p><span class="status present">${esc(p.data.role)}</span></div></div></main>`}init();